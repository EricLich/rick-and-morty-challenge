const Header = () => {
  return (
    <div className="w-full h-[80px] fixed top-0 z-50 border-b border-main shadow-border-shadow text-main font-sono drop-shadow-fontShadow flex justify-center items-center">
      <nav className="w-full h-full max-w-[1200px] m-auto flex items-center">
        header
      </nav>
    </div>
  );
};

export default Header;
